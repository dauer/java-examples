package switchexpr.patternmatching;

/**
 * Available from Java 20 preview...
 * TODO I can't seem to get IntelliJ to accept preview features?...
 */
public class PatternMatchingSwitchExample {
/*
    public static String process(Object message) {
        return switch(message) {
            case Integer i when i == 7 -> "Got a seven"; // Example of a guard: 'when i == 7'
            case Integer i -> "Got a number " + i;
            case String str -> "Got a string of length " + str.length();
            default -> "Whatever";
        };
    }

    public static void main(String[] args) {
        System.out.println(process(1));
        System.out.println(process(7));
        System.out.println(process("Hello world"));
        System.out.println(process(null));
    }
*/
}
